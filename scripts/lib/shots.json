{
  "_comment": [
    "Which frame from `npm run shoot` ships where. One list, because the same",
    "frames feed four surfaces and they drifted apart: a new shot landed with no",
    "home, a deleted frame stayed referenced, and the store set fell five weeks",
    "behind the README set.",
    "",
    "surfaces: readme = docs/screenshots/ (2880x1800, README.md)",
    "          landing = docs/index.html (reads the readme set's files)",
    "          store = docs/store-screenshots/ (1280x800, both extension stores",
    "                  and the Microsoft Store listing)",
    "",
    "`composed: true` means the frame is not photographed but built from others",
    "(compose-tricolor.cjs), so the downscaler must not look for a source of the",
    "same name. `sourceFor` names the frames a composed one is built from: they",
    "are shot every run but ship nowhere themselves. A frame with `ships: []` is",
    "also still shot every run — the shoot doubles as a UI smoke test — it just",
    "must not sit committed in a shipped folder.",
    "",
    "`reading` became one of those on 2026-08-08, when tricolor took over as the",
    "opening frame everywhere. The rule behind that (Emil's): a frame that OPENS",
    "a surface has to read as an application, so tricolor keeps its toolbar — and",
    "since tricolor is composed FROM `reading`, `reading` has to keep its toolbar",
    "too. The toolbar-less frame the copy needs is therefore `page_only`, shot",
    "separately, and `reading` stayed on as a tricolor source only.",
    "",
    "`page_only` stopped shipping on 2026-08-09 (Emil). A picture of a page with",
    "no application in it cannot carry the reading section: three of the four",
    "frames on the landing page show the app, and the fourth showed its absence —",
    "next to Edge's viewer it proved nothing. The reading section now sits",
    "directly under tricolor and uses THAT as its illustration, so it needs no",
    "frame of its own. page_only is still shot every run as a smoke test.",
    "",
    "`shortcuts` is new and ships NOWHERE yet, on purpose: Emil approved the",
    "idea of a frame for the keyboard map (2026-08-09) but every shipped image",
    "is his to shoot and approve. The recipe drives the app into the state; the",
    "frame lands here once he has run `npm run shoot` and picked it. When it",
    "does, it goes LATE in a page — never as an opening frame. A settings",
    "dialog is a thing you go and find, not what the app looks like.",
    "",
    "Read by scale-store-shots.cjs (what to downscale) and check-screenshots.mjs",
    "(what must exist where)."
  ],
  "frames": {
    "reading": { "ships": [] },
    "page_only": { "ships": [] },
    "reading_tabs": { "ships": [] },
    "tricolor": { "ships": ["readme", "landing", "store"], "composed": true, "sourceFor": ["reading", "parchment", "night"] },
    "annotations": { "ships": ["readme", "landing", "store"] },
    "annotations_edit": { "ships": [] },
    "feedback": { "ships": [] },
    "signature": { "ships": [] },
    "margin": { "ships": ["readme"] },
    "shortcuts": { "ships": [] },
    "dual-pane": { "ships": ["readme", "landing", "store"] },
    "assistant": { "ships": ["readme", "landing", "store"] },
    "assistant_snip": { "ships": ["readme"] },
    "assistant_figure": { "ships": ["readme", "store"] },
    "parchment": { "ships": [] },
    "night": { "ships": [] },
    "night+": { "ships": [] }
  },
  "storeOrder": ["tricolor", "annotations", "assistant", "assistant_figure", "dual-pane"]
}
